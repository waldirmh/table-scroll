  {
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Web",
            "type": "node-terminal",
            "request": "launch",
            "command": "zsh -ic 'nvm use 22 && npm run dev'",
            "cwd": "${workspaceFolder}"
        }
    ]
}

